<% content_for :title, "Edit Packaging" %>
<div class="max-w-2xl mx-auto">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-gray-900">Edit Packaging: <%= @packaging.name %></h1>
    <%= link_to "Kembali", packaging_path(@packaging), class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
  <%= render 'form', packaging: @packaging %>
</div>
